# Required runtime and scanner dependencies staged for the network-disabled
# appliance application build. Regenerate application-requirements.lock with
# Python 3.12 on Linux and pip-tools after reviewing every version change.
-r ../services/agent/requirements.lock
-r ../services/search-mediator/requirements.lock
-r ../services/ui/requirements.lock
-r ../services/quarantine/requirements.lock

pip==26.1.2
setuptools==83.0.0
wheel==0.46.2

modelscan==0.8.8
fickling==0.1.12
modelaudit==0.2.42
huggingface-hub==1.25.1

# Runtime dependencies declared by the exact SearXNG source commit pinned in
# .upstreams.lock.yaml. SearXNG itself is installed from that reviewed local
# source with --no-deps after this complete wheel graph is installed.
# certifi is supplied by the application lock at its reviewed current patch
# release rather than SearXNG's older exact patch pin.
babel==2.18.0
flask-babel==4.0.0
flask==3.1.3
jinja2==3.1.6
lxml==6.1.1
pygments==2.20.0
python-dateutil==2.9.0.post0
pyyaml==6.0.3
httpx[http2]==0.28.1
httpx-socks[asyncio]==0.10.0
sniffio==1.3.1
valkey==6.1.1
markdown-it-py==4.2.0
msgspec==0.21.1
typer==0.27.0
isodate==0.7.2
whitenoise==6.12.0
typing-extensions==4.16.0
