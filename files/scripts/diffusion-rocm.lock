#
# Backend-specific diffusion runtime lockfile: ROCm 6.0
#
# STATUS: UNPOPULATED — this file is structural scaffolding only.
# It cannot be used for installation until populated with real hashes.
#
# To generate:
#   scripts/refresh-diffusion-locks.sh --backend rocm
#
# This runs pip-compile with:
#   pip-compile --allow-unsafe --generate-hashes \
#       --extra-index-url https://download.pytorch.org/whl/rocm6.0 \
#       --output-file=diffusion-rocm.lock diffusion-in.txt
#
# Once generated, the lockfile is consumed by the installer in offline mode:
#   pip install --no-index --require-hashes --find-links=<verified-cache> -r diffusion-rocm.lock
#
